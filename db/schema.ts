import { pgTable, serial, text, integer, boolean, timestamp, real } from "drizzle-orm/pg-core";
export const universities = pgTable("universities", { id: serial("id").primaryKey(), name: text("name").notNull(), nameRu: text("name_ru").notNull(), city: text("city").notNull(), country: text("country").notNull(), website: text("website"), isKazakh: boolean("is_kazakh").default(false).notNull(), ranking: integer("ranking"), createdAt: timestamp("created_at").defaultNow() });
export const programs = pgTable("programs", { id: serial("id").primaryKey(), title: text("title").notNull(), titleRu: text("title_ru"), universityId: integer("university_id").references(() => universities.id).notNull(), field: text("field").notNull(), language: text("language").array().notNull(), cost: integer("cost").notNull(), costUsd: integer("cost_usd"), duration: text("duration").default("2 года"), deadline: text("deadline"), bolashak: boolean("bolashak").default(false).notNull(), tags: text("tags").array().default([]), description: text("description"), requirements: text("requirements").array().default([]), documents: text("documents").array().default([]), rating: real("rating").default(4.0), students: integer("students").default(0), createdAt: timestamp("created_at").defaultNow(), updatedAt: timestamp("updated_at").defaultNow() });
export const users = pgTable("users", { id: serial("id").primaryKey(), email: text("email").notNull().unique(), name: text("name"), password: text("password"), role: text("role").default("user"), createdAt: timestamp("created_at").defaultNow() });
export const favorites = pgTable("favorites", { id: serial("id").primaryKey(), userId: integer("user_id").references(() => users.id).notNull(), programId: integer("program_id").references(() => programs.id).notNull(), createdAt: timestamp("created_at").defaultNow() });
export const checklistItems = pgTable("checklist_items", { id: serial("id").primaryKey(), userId: integer("user_id").references(() => users.id).notNull(), programId: integer("program_id").references(() => programs.id), docName: text("doc_name").notNull(), isDone: boolean("is_done").default(false).notNull(), category: text("category"), createdAt: timestamp("created_at").defaultNow() });
export const applications = pgTable("applications", { id: serial("id").primaryKey(), userId: integer("user_id").references(() => users.id).notNull(), programId: integer("program_id").references(() => programs.id).notNull(), status: text("status").default("draft"), notes: text("notes"), createdAt: timestamp("created_at").defaultNow(), updatedAt: timestamp("updated_at").defaultNow() });
export const notifications = pgTable("notifications", { id: serial("id").primaryKey(), userId: integer("user_id").references(() => users.id).notNull(), programId: integer("program_id").references(() => programs.id).notNull(), sendAt: timestamp("send_at").notNull(), type: text("type").default("deadline"), sent: boolean("sent").default(false).notNull(), createdAt: timestamp("created_at").defaultNow() });